{ config, lib, pkgs, ... }:

{
  # FIXME virtualbox is marked broken
  # virtualisation.virtualbox.host.enable = true;
  # virtualisation.virtualbox.host.enableExtensionPack = true;
  # users.extraGroups.vboxusers.members = [ "jj" ];
}
